# forge 协作开发规范

本文面向项目开发者和参与开发的 AI。目标是让每项改动都有明确意图、独立边界、可复现验证和可追溯评审。

## 基本原则

- `main` 是唯一长期分支，并应始终保持可运行。
- 不建立长期个人分支、模块分支或 `develop` 分支。
- 一个 Issue 对应一个短期分支和一个 PR。
- Issue 按用户可感知结果拆分，不按代码文件拆分。
- 文档、实现和实际产品表现必须保持一致。
- AI 可以实施、测试、提交、推送功能分支并创建 PR，不能绕过检查直接修改 `main`。
- 最终发布和生产部署由项目负责人执行。

## 当前 GitHub 执行状态

仓库已设为私有，并已启用 Issue、PR、Actions、Squash Merge 和合并后自动删除分支。当前 GitHub Free 套餐不支持私有仓库 Branch Protection，因此平台暂时不能强制阻止直接推送 `main`，也不能把检查和批准设成技术门禁。

在升级 GitHub Pro 前，所有成员必须按本文自律执行：只向功能分支推送，通过 PR 合并，并在自动测试与构建通过、非作者评审后再合并。不要因为平台暂时不拦截就绕过流程。

升级和保护规则配置跟踪见 [Issue #4](https://github.com/RexYoung000/editor/issues/4)。不采用“把内部仓库改回公开”换取免费分支保护的方案。

## 1. 创建 Issue

开发开始前先创建 Issue。Issue 至少说明：

- 为什么要做，解决谁的什么问题。
- 用户会看到或感受到什么变化。
- 本次包含和不包含什么。
- 如何验收，包括关键正常场景和异常场景。
- 已知依赖、风险或需要先确认的决策。

需求仍有多种实质解读时，不进入实施。每轮只对齐一个最关键问题，确认后再继续。

小型拼写修正和纯维护任务也应有 Issue；紧急线上处理可以事后补录，但不能让改动失去来源。

## 2. 创建分支

从最新 `main` 创建短期分支：

```bash
git switch main
git pull --ff-only origin main
git switch -c feat/23-preset-s4-s7
```

命名格式为 `<type>/<issue-number>-<short-name>`：

| 类型 | 用途 | 示例 |
| --- | --- | --- |
| `feat` | 新能力或用户体验变化 | `feat/23-preset-s4-s7` |
| `fix` | 缺陷修复 | `fix/31-export-image` |
| `docs` | 文档与协作配置 | `docs/12-workflow` |
| `refactor` | 不改变用户能力的结构调整 | `refactor/45-export-pipeline` |
| `chore` | 依赖、脚本和维护 | `chore/52-upgrade-vite` |

不要在同一分支混入无关 Issue。需要依赖另一项未合并工作时，应在 Issue 和 PR 中明确依赖关系。

## 3. 开发顺序

1. 阅读根目录 `AGENTS.md`、Issue 和相关模块文档。
2. 检查当前分支、工作区和远端状态。
3. 中等以上改动先说明方案、影响和风险。
4. 先更新承载本次决定的现有文档。
5. 依据对齐结果实施，避免顺手重构无关模块。
6. 添加与风险相匹配的自动测试。
7. 执行自动检查和真实界面验收。
8. 将实施中产生的新限制和决定回写文档。
9. 提交并推送当前功能分支。
10. 创建 PR，等待检查和评审，不直接合并 `main`。

## 4. 提交规范

提交信息使用中文，并采用以下格式：

```text
<type>(<scope>): <具体结果>
```

示例：

```text
feat(preset): 增加 S4-S7 通用练习页面模板
fix(export): 修复模板图片发布后路径失效
docs(workflow): 补充 Issue 和 PR 协作规范
test(preset): 增加模板资源完整性检查
```

可用类型：`feat`、`fix`、`docs`、`test`、`refactor`、`chore`、`build`、`ci`。

提交信息必须让不阅读代码的人也能理解结果。禁止使用“更新代码”“优化项目”“修改问题”等模糊描述。开发分支允许有多个逻辑清晰的提交，主分支通过 Squash 保留一个 Issue 对应一个提交。

## 5. 提交前检查

最低自动检查：

```bash
pnpm lint
pnpm test
pnpm build
```

涉及界面、画布、Electron、本地文件、预览或导出时，还必须执行对应的真实体验验收，并在 PR 中记录入口、操作路径和结果。

三项检查都必须通过。不得通过关闭规则、扩大 ignore 范围或降低类型约束制造“通过”。

## 6. 创建 PR

PR 标题沿用提交格式，并在正文中写 `Closes #<issue-number>`。PR 必须说明：

- 用户或产品层面的实际变化。
- 本次明确不包含的内容。
- 影响模块和主要风险。
- 自动测试、构建和人工验收结果。
- 文档是否已同步。
- 截图、录屏或真实体验入口（视觉改动必填）。

PR 应保持可评审。范围明显超过一个 Issue 时，应先拆分，而不是让评审者面对一个无法判断边界的大改动。

## 7. 评审与合并

合并前必须满足：

- PR 关联的 Issue 已对齐。
- GitHub 自动测试和构建通过。
- 所有评审讨论已解决。
- 至少一名非作者完成审核；关键模块由 CODEOWNERS 审核。
- 文档、代码和实际表现一致。

默认使用 Squash Merge。Squash 提交标题必须遵循提交规范。任务只有在 PR 已合并、远端和本地任务分支均完成清理后，才算完成。

GitHub 会自动删除通过 PR 合并的远端分支，但不会删除开发者电脑上的本地分支。执行合并的人负责完成收尾；如果在 GitHub 网页合并，下一次进入该仓库的 AI 或开发者必须在开始新任务前完成以下步骤：

1. 切回 `main`，执行 `git fetch --prune origin` 和 `git pull --ff-only origin main`，同步合并结果和远端分支状态。
2. 确认工作区干净，并确认待删除分支不存在未推送或未进入 PR 的成果。
3. 删除对应本地任务分支；Squash Merge 后不能只依赖提交祖先关系判断是否已合并，应同时核对 PR 状态和最终文件内容。
4. 检查本地与远端分支列表，只保留 `main` 和仍在进行的短期任务分支；发现未被 GitHub 自动删除的已合并远端分支时一并清理。

不得为了整理列表删除未合并分支、含未推送成果的分支，或来源不明的分支。无法确认时先保留并向项目负责人说明。

禁止强推 `main`、覆盖提交历史或绕过协作规则。确需紧急处理时，由项目负责人说明原因和影响，并补齐 Issue、测试与复盘记录。

## 8. 版本与发布

- 普通 Issue 和 PR 不修改版本号。
- 发布时单独创建版本 Issue 和发布 PR。
- 版本号遵循 SemVer，规则见 `docs/versioning.md`。
- 发布 PR 完成版本号、回归测试、打包前置检查和发布说明。
- 合并发布 PR 后创建 `vX.Y.Z` Tag 和 GitHub Release。
- 最终 Electron 打包、分发或生产发布由项目负责人执行。

## 9. AI 协作边界

- AI 先读取仓库事实，不凭空补全需求。
- 需求存在多种解读时，列出方向并等待产品决策。
- 不提交密钥、本机绝对路径、生成目录或个人工具状态。
- 不通过关闭测试、降低类型约束或吞掉错误来制造“通过”。
- 不删除、重置或覆盖他人的未提交改动。
- 两次尝试仍失败时，停止小修小补，说明根因和下一步选择。
