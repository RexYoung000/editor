# 测试与验收

forge 同时包含纯逻辑、React 界面、Laya 画布、Electron 本地文件和远程预览链路。单一测试手段不能覆盖全部风险，因此 PR 需要按改动范围组合自动检查和人工验收。

## 合并检查

所有 PR 至少执行：

```bash
pnpm lint
pnpm test
pnpm build
```

GitHub Actions 会对进入 `main` 的 PR 自动执行这三项检查。失败时不能通过修改测试、吞掉错误或降低约束绕过。

当前私有仓库使用 GitHub Free，Actions 可以运行，但 GitHub 暂时不能把检查设为 Branch Protection 的必需状态。升级 GitHub Pro 前，由评审者确认检查通过后再执行 Squash Merge；技术门禁配置跟踪见 [Issue #4](https://github.com/RexYoung000/editor/issues/4)。

## 四层验证职责

涉及交互设计时，根据不确定性和改动范围组合以下四层验证。它们解决的问题不同，不能互相替代：

| 验证层 | 主要用途 | 不负责验证 |
| --- | --- | --- |
| 对话内交互 Demo | 在实装前即时体验拖拽、吸附、动画、空间布局、连续数值、复杂状态切换，以及信息层级和视觉反馈 | 项目代码集成、Laya/Electron 边界、真实性能、本地文件和导出结果 |
| 浏览器真实页面 | 验证 React 页面、样式、响应式布局、浏览器事件和可访问的异常状态 | Electron IPC、桌面窗口行为和仅在 Laya 运行时成立的结果 |
| Electron 真实环境 | 验证 Laya 画布、桌面交互、本地文件、远程连接、预览导出和完整集成体验 | 不能替代可重复运行的逻辑回归保护 |
| 自动测试与构建 | 稳定保护纯逻辑、数据兼容、边界条件、历史事务、类型和构建结果 | 真实手感、视觉密度、动画节奏和桌面环境差异 |

存在实质交互不确定性时，设计预验收流程为：

1. 先对齐要解决的用户问题和本次范围。
2. 制作只覆盖关键判断的最小可操作 Demo，不复制完整产品。
3. 由 Rex 即时体验并确认参数、状态、交互规则和视觉反馈。
4. 将确认结果写回对应需求或设计文档，再开始项目实装。
5. 实装后继续执行自动检查，并按改动范围完成浏览器或 Electron 真实验收。

小型修复、规则明确的界面调整和无需体验判断的任务不强制制作 Demo。Demo 文件不进入产品源码或正式交付物；它用于提前收敛设计理解，不能替代最终集成验收。Issue #15 的 Axure 式动态距离提示是首次应用：先通过可拖拽 Demo 确认“两元素实时测距 + 三元素等距增强”，再回写设计文档并修改真实画布。Issue #52 通过完整图层面板拖拽 Demo 进一步确认“自由放置为主、弱磁吸为辅”、拖动与浮动透明度、取消拖动和窗口约束，再进入项目实装。

## 现有自动测试

`pnpm test` 通过 `scripts/run-unit-tests.mjs` 编译并运行 `tests/*.test.ts`。当前覆盖重点包括：

| 范围 | 主要验证 |
| --- | --- |
| 画布选择与命中 | 旋转命中、框选、父子选择、容器穿透 |
| 画布变换 | 多选移动、缩放、旋转、拖动复制和历史记录 |
| 图片镜像 | 左右/上下单次与重复镜像、双向组合、旋转和自定义锚点下的坐标补偿、单图与多选边界、锁定限制、撤销重做，以及正式与预习导出一致性 |
| 图层显隐与锁定 | 自身状态、父级多层继承、父级解除后的子级恢复、锁定选择与变换限制、批量操作和单条历史 |
| 编辑器图层组 | 显式组与旧 `groupId` 选择边界、整行点击热区、成员独立框选/变换/删除、组级显隐/锁定批量操作、组外拖放、同运行父级约束、循环保护、组 ID 重建、复制粘贴、嵌套清理和单条历史 |
| 画布视口 | 缩放、平移、适应画布和可见边界 |
| 容器几何 | 越界判断、移回容器和扩展容器 |
| 内部页面 | 增删复制、页面分组兼容、插入排序、动作关系、跨关卡移动、弹窗编辑态底板与遮罩合成和编译 |
| 事件与 SDK 判定 | 触发元素与目标分离、兼容目标过滤、结果分支、失效引用、历史事件兼容和导出代码；正课、预习、作业与专题测评必须同时覆盖普通事件监听和动作代码，同一事件只绑定一次，`onClickSound` 每次触发只注入一次点击音效，复习课保持纯视频边界 |
| 图层面板布局 | 持久化状态归一化、自由浮动边界约束、尺寸上下限、弱磁吸命中和恢复默认 |
| 关卡缩略图与画布生命周期 | 500ms 防抖及连续变化重置、立即提交与旧任务取消、折叠不改变当前选择，以及课程 ID / 页面 ID 渲染上下文边界 |
| 属性面板输入 | 文本与有效数值实时写入课程状态、未完成数值草稿隔离、500ms 连续输入合并、失焦/切换/保存强制提交，以及一次编辑一次撤销 |
| 自定义答案配置热区 | 每个答案输入框的视觉区域与真实命中区域一致，序号及相邻空隙可代理输入焦点，操作按钮不覆盖输入焦点区，答案 1 到答案 2 可直接切换，空值校验不阻止聚焦 |
| 输入框字体图与字号 | 普通输入框与分数输入框共用三色样式和字形生成规则，新建默认蓝色，旧元素保留既有字体图；字号基准、框体同比缩放、应用/取消、测试样例隔离、自定义答案字符集合和预览/导出资源一致性 |
| 文本工具 | 创建后定位在可见画布中央、显示真实默认内容“**双击编辑文本**”并保持普通选中态，不自动进入输入；点击外部不删除组件；双击默认内容全选、双击其他文本按点击位置放置光标；清空退出仍保留组件；I-beam、高对比原生插入光标、局部选区、中文输入法、`Cmd/Ctrl+Enter`/`Esc` 退出；选中态拖动移动而编辑态拖动不移动；三种尺寸模式的手柄数量与自动高度；固定宽高溢出橙色提示；选中态与编辑态辅助框在深浅背景、旋转、透明度和不同缩放下清晰可辨；一次编辑会话一次历史；默认内容和用户文字在画布、数据、缩略图、预览与导出中一致 |
| 字体库与兼容 | 单层预设列表、字体 ID 与资源一一对应、思源黑体 Regular 默认值、旧方正与未知字体 ID 迁移、最近使用字体归一化、本地字体优先级，以及编辑、预览和导出共用同一字体解析结果 |
| 关卡结构与复制 | 普通课件的预习与正课都支持多小关卡；复制小关卡紧跟原页，复制大关卡紧跟原关卡并完整重建普通页、内部页面、元素、动作、分组和引用 ID；视频大关卡可整体复制但不能新增小关卡；作业、专题测评和复习课边界不变 |
| 导出结果 | 正常课、作业、专题测评、预习、复习课和内部页面的场景、代码、配置与资源路径，以及正式与预习共享场景规则的一致性；预习与正课的每个普通大关卡都必须按小关卡顺序生成等量 `subviews`，不得只生成第一页；含输入框时，正课、作业、专题测评与预习配置必须加载对应 `inputImg.atlas`，复习课保持纯视频配置；画笔组合必须导出 SDK 可创建的 `BrushSprite` 运行节点，旧 `game/image/img/*` 资源路径不得生成重复的 `img/img` |
| 模板与资源 | 自定义模板格式、资源扫描和 ID 重建；预设模板的关卡结构归属、课件类型过滤、实例 ID 独立、稳定展示名称、内置资源完整性，以及卡片预览与母版元素使用同一数据源；新增模板出现未支持组件或视觉属性、重复 ID、无效或循环父子关系、无效几何变换、未注册皮肤时检查失败 |
| 资源库 | 快捷标签、目录聚合、模糊搜索和系列识别 |
| 预览路由 | 正课与预习课程标识映射到共享上传目录，忽略 GameLoader 缓存参数，并保护普通课程名和真实缺失路径 |
| 课件另存事务 | 同 ID 跨路径、新 ID 改名、规范化路径关系、空目录/有效课件/非课件目录识别、源目录内部目标与越界符号链接保护、完整替换不残留旧文件、事务中断恢复、源版本管理信息排除、目标版本管理信息保留，以及冻结编辑状态的深层副本、活动路径和资源缓存切换 |

新增纯逻辑或修复可复现缺陷时，应优先补单元测试。测试名称使用用户可理解的行为描述，不只描述内部函数。

## 构建检查

`pnpm build` 执行：

1. `tsc -b`：检查 TypeScript 类型和工程引用。
2. `vite build`：验证前端可以生成静态构建产物。

构建通过不代表 Electron、本地文件 API 或远程预览可用。

## lint 检查

`pnpm lint` 对 TypeScript、React Hooks、React Fast Refresh 和项目约定执行全量静态检查，并作为 CI 合并检查。

每个 PR 必须遵守：

- lint 零错误。
- 不关闭规则或扩大忽略范围来规避问题。
- 不用 `any`、不安全断言或吞掉异常替代真实类型设计。
- 类型治理不得顺手改变无关产品行为。

## 人工验收矩阵

| 改动范围 | 必须验收 |
| --- | --- |
| 普通 React 界面 | 浏览器关键路径、空状态、异常状态和不同窗口尺寸 |
| 图层面板布局 | 浏览器验证完整面板拖拽、任意位置自由放置、弱磁吸、三种停靠、各模式尺寸调整、收起、`Esc` 取消、恢复默认和窄窗口约束；长列表超出可视高度时，浮动、左侧、右侧和底部停靠模式均可在内容区独立上下滚动，标题栏不随内容滚动且拖拽排序仍可用；图层面板移动到全局弹窗区域时，弹窗遮罩和内容始终覆盖面板并阻断其操作；验证实际外层标题栏始终提供创建空图层组入口，未选择图层时也可点击；Electron 验证切换关卡与专注工作区、重启恢复布局、真实画布空间变化且始终只有一个面板 |
| 图层显隐与锁定 | Electron 验证图层行眼睛/锁定控件、父级隐藏/锁定对子级的继承来源、父级恢复后子级自身状态保持、锁定对象可选中查看但无变换手柄且不能拖动，属性面板编辑区域整体置灰，以及普通页与内部页面共用面板 |
| 编辑器图层组 | Electron 验证无需预选图层即可创建空组，再通过拖拽或组内移入整理成员；验证空组和有成员的组都可选中并同步高亮成员，点击成员行名称/类型/留白都能切换，单击组内成员只选该成员，单击组外元素一次即可切换；组右侧验证隐藏/显示组成员、锁定/解锁组成员，包含嵌套组且每次只写一条历史；成员可独立移动/缩放/旋转/删除，框选只包含实际命中成员；验证成员拖到普通层级会解除组归属且一步撤销可恢复，拖入其他组只更新一次归属；重命名/排序/解散/删除在右侧属性面板执行；同时验证旧 `groupId` 仍整组联动、图层组与运行容器视觉区分、展开状态、撤销恢复，以及预览和导出结构不变 |
| 点击触发 SDK 判定 | 浏览器验证普通按钮、图片、文本均可添加判定事件，目标只显示当前页兼容题型，独立输入格可直接填写正确答案，切换目标后答案摘要与正确/错误/未完成分支同步；Electron 依次预览独立输入格、填空、选择、拖拽和连线目标，验证正确、错误、未完成的真实结果与目标删除后的失效提示，并回归历史 ConfirmButton 快捷题型 |
| 选择题配置与交互 | 浏览器验证容器内选项增删改名、稳定答案引用、自动单选/多选推导、重复答案过滤和发布校验；Electron 验证文字点击穿透、单选替换与取消、多选自由勾选、通常/按压/选中/正确/错误状态替换、错误后重选、正确后锁定，以及正课确认按钮和线上作业“修改 / 完成 / 跳过”流程；真实导出验证正课、预习、作业和专题测评一致 |
| 专注工作区页面管理 | 浏览器验证统一列表、分组、搜索筛选和收起状态；Electron 验证拖拽命中、占位动效、自动滚动、快捷新增、撤销与保存重开 |
| 内部页面工作台入口 | 浏览器验证常显“进入编辑”、悬停快捷按钮、文字化更多操作和窄侧栏容纳；Electron 验证工作台画布只读、点击提示、按钮/双击/Enter 进入、返回后恢复只读，并确认普通小关卡仍可直接编辑 |
| 弹窗编辑与预览 | Electron 验证主界面底板、遮罩、弹窗内容三层顺序；编辑态依次检查遮罩 0%、55%、100% 和颜色切换，并确认遮罩不可选中且不进入图层列表；正课与预习真实预览确认遮罩颜色、透明度和底板拦截与编辑态一致 |
| 关卡缩略图与折叠画布 | Electron 在预习普通页、正课普通页和视频页依次新增、移动、缩放、删除元素及修改属性，确认停止操作 500ms 后当前缩略图自动更新，连续操作期间不逐帧截图；分别收起整个预习区、正课区和当前大关卡，确认选择、属性面板和画布不变，展开后再次点击当前页无黑屏或其他页面残留；反复跨区域切页、保存并进入/返回预览，确认最后画面被提交且真实切换课件仍会正确清理重建 |
| 属性面板直接输入 | 浏览器验证 X/Y、宽高、旋转、组件数值、单行文本和多行文本输入时画布与课件状态实时更新；空值、负号和小数点等未完成数值不污染课程；连续输入停止、Enter、失焦和切换对象后只生成一条历史。Electron 验证输入未失焦时执行 `Ctrl/Cmd+S`、文件菜单保存、自动保存、切换页面和返回首页，重新打开课程仍保留最后一个有效值，并验证一次撤销/重做恢复整次编辑 |
| 自定义答案键盘配置 | 浏览器和 Electron 验证答案至少保留 2 项且可持续新增，不显示固定上限；用 10、20、50 项验证配置、自动分行和画布更新无明显卡顿，保存重开、预览与正式导出不截断。分别构造清空键与答案同排、清空键单独成行及末行剩余多个普通键宽度的布局，确认答案按顺序优先向前排满，清空键九宫格从当前位置拉伸至底板右侧内容边线、单独成行时铺满整行，清空图标保持居中且不变形；编辑画布、预览和正式导出一致。答案较多导致键盘超出画布时展示真实结果，由老师自行减少数量，不增加分页、滚动或二次上限。鼠标进入每个深色输入框的上、下、左、右边缘及中部均显示文本光标并可聚焦；开启系统大光标后，点击序号、序号与输入框之间的空隙也会聚焦对应答案，从答案 1 直接点击答案 2 无需空白区域；空值红色校验、上移/下移/删除/新增操作和窄面板布局无回归 |
| 数学键盘预设与主题 | 浏览器验证八种数学键盘在选择器中的名称、黄色缩略图、排列顺序和输入框兼容类型，并逐项切换黄、蓝、绿三色色块；Electron 验证 3 列 4/5 行和 5 列 4 行布局、跨列删除键、普通/按下态、实际点击热区与输出字符。编辑器画布、Electron 预览和真实导出逐一确认底板完整九宫格、退格图标不拉伸、无超框裁切，并确认旧课件缺少主题字段时仍使用黄色。 |
| 输入框字体图与字号预览 | 浏览器分别新建普通输入框和分数输入框，确认默认蓝色且可独立切换黄、蓝、绿，改变输入框主题不改变键盘主题，反向亦然；打开字号与间距弹窗，确认左侧使用当前框体尺寸和 `10,10,10,10` 九宫格，普通文本与可视化分数编辑即时更新，测试文本受输入位数与可输入字符约束，`12–120px` 主字号及 `-20–60px` 字符间距数值框都支持清空重输并与滑杆双向同步。分别收紧和加宽间距，确认一个值同步控制普通连续字符、分数框外层字符、分子分母及字符与分数结构之间的间隔，负值不导致字符顺序错乱或结构宽度为负。分数输入框还要验证 `40%–100%` 的分数字号比例只改变分子分母，并同步改变分数线宽度、上下间距和结构占位；使用 `12<3_4>` 和多位分子/分母检查分数结构按实际位数动态收放、连续数字间距自然、内层只显示轻量编辑焦点且不越出外层框体。输入规则、两套字号与字符间距随“应用”一起保存，“取消”完全保留原值。Electron 将框体按比例缩放后重新打开弹窗，确认普通字符、运算符、字符间距和分数结构同比缩放且实际值更新；绑定自定义答案键盘后确认答案变化只更新字符集合，不覆盖输入框主题。保存重开、正课/预习预览和真实导出中应保留颜色、字号与间距，测试样例不得出现在画布、答案或课件中；打开无新主题字段的历史课件时继续使用原字体图。 |
| 文本工具行内编辑 | 浏览器和 Electron 分别在白底、黑底、彩色与复杂图片背景下，以低倍率、100% 和高倍率创建文本，确认显示真实默认内容“**双击编辑文本**”、新文本默认白色且在标准黑色画布上可读、保持蓝白普通选中态并且不自动聚焦；点击画布、属性面板、工具栏和其他组件不会删除文本；双击默认内容后全部选中并可直接替换，双击其他文本时光标落在点击位置；清空后通过点击外部、`Cmd/Ctrl+Enter` 或 `Esc` 退出仍保留组件；回归 I-beam、高对比插入光标、中文输入法、选区、换行和原生撤销，并确认保存重开、缩略图、预览和导出包含真实文本但不包含编辑辅助视觉 |
| 图片镜像 | Electron 单独选中普通图片，在右侧“变换”区域分别点击左右与上下镜像，确认按钮提示、即时画布结果、同方向执行两次恢复、双向组合、一步撤销/重做和保存重开；再以旋转图片及非居中锚点重复操作，确认图片沿自身方向镜像且位置、外框、选区和控制点不跳动；多选、混合选择和锁定图片不得执行镜像；正课与预习的真实预览、导出结果必须与编辑画布一致 |
| Laya 画布与组件 | Electron 中的添加、选择、变换、撤销和预览表现；画笔组合预览初始只显示画笔开关，点击后清空按钮与画板同时出现并可绘制，控制台不得出现运行类创建失败、资源 404 或空节点异常 |
| 本地文件与模板 | Electron 打开真实课程、保存、重启后读取和资源去重；另存时验证同 ID 跨目录、新 ID、同路径、源目录内部目标、空目标、有效同名课件替换、非课件目录拒绝、跨磁盘、大视频、文件占用、无写入权限与中断恢复，确认原课件和取消/失败时的原目标不受损，`.svn`/`.git` 不从源目录复制且目标原有信息保留；使用包含预习关卡的真实编辑状态验证目标副本写入后能切换为可继续编辑的独立对象，若仅界面切换失败则提示目标副本已经写入而非笼统的另存失败；另存成功后顶部立即显示末尾两级目录，悬停显示完整路径、点击打开文件夹，窄窗口不挤压右侧工具，资源预览来自新活动路径；逐项对比所有内置预设卡片与创建后的真实画布，确认背景、装饰、文本、层级和位置一致；普通课件分别从预习与正课创建木纹卷轴单页面和内部关卡，确认布局、锁定、文本编辑与音频选择 |
| 远程模式 | Vite 与 Electron 分别运行，确认没有发送本机绝对路径 |
| 预览与导出 | 生成课件、上传预览、GameLoader 打开和关键交互；普通课件分别验证预习与正课的多小关卡大关卡、整关副本和视频整关副本，确认默认显示第一个小关卡并可沿现有上下切换浏览全部 `subviews`；资源进度达到 100% 后启动层应退出并显示首屏，不能停留在“正在启动” |
| 内置资源 | 编辑器显示、导出路径和课件包资源存在性；修改 `public/builtin/runtime/game/` 后执行 `pnpm pack-game` 并核对 `game.zip` |
| Electron 打包 | Windows 安装包启动、连接服务器和相关 IPC |

PR 只需要执行与风险相关的项目，但必须说明未覆盖项。

## PR 验收记录

PR 中应写清楚：

- 自动执行了哪些命令及结果。
- 人工验收入口和操作路径。
- 应看到的结果及实际结果。
- 未覆盖的环境或场景。
- 已知限制、偏差或后续 Issue。
- 视觉改动的截图或录屏。

不要只写“测试通过”。评审者应在不阅读代码的情况下判断改动是否达到 Issue 目标。

## 发布前回归

版本发布除 PR 门禁外，还需要：

1. 对照版本 Issue 回归所有用户可感知变化。
2. 在真实 Electron 环境打开、编辑和保存课程。
3. 验证至少一种正常课件的预览与发布。
4. 验证本版本涉及的课程类型和资源类型。
5. 执行 `pnpm electron:build` 并检查安装包启动。
6. 在 Windows 上实际安装 NSIS 安装包，验证首次安装、覆盖安装、服务器连接和本版本涉及的关键 IPC。
7. 使用与正式部署等价的 Vite 服务完成局域网 Electron 连接、资源库、真实课程保存、预览和发布冒烟检查。
8. 核对编辑器显示版本、安装包版本、目标 Tag 和 Release 版本一致。
9. 记录未解决问题、未覆盖环境、素材快照和回退方式。

最终发布由项目负责人执行。

完整的发板、公司台式机部署、老师端交付和回退流程见 [正式发板与内部部署](./internal-deployment.md)。
