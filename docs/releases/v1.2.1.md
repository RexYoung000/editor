# forge v1.2.1 发布记录

## 版本信息

| 项目 | 内容 |
| --- | --- |
| 目标版本 | `v1.2.1` |
| 发布日期 | `2026-07-23` |
| 代码基线 | 发布 PR 合并后的 `v1.2.1` Tag |
| 客户端升级 | 无需升级；原 v1.2.0 客户端重启后继续连接 v1.2.1 服务器 |
| 素材库 | 沿用 v1.2.0 使用的素材快照，不新增素材与标签映射 |
| 回退版本 | `v1.2.0` |
| Version Issue | [#97](https://github.com/RexYoung000/editor/issues/97) |
| 发布 PR | 待创建 |

## 用户可感知变化

- 文本工具在当前可见画布中央创建，新建后直接进入行内编辑。
- 文本支持自动宽高、固定宽度自动高度和固定宽高三种尺寸模式，并改善光标、选区、中文输入法和画布撤销体验。
- 编辑、画布显示、预览和导出 PNG 共享文字测量与换行规则，降低预览或发布后的换行和裁切偏差。

## 修复

- 修复插入分数后点击分数输入框导致预览卡死。
- 分子或分母获得焦点时禁用分数按钮，防止嵌套分数结构。
- 修复带内容分数整块删除后留下残缺编码和渲染残影。
- 补齐带整数部分的分数令牌解析，并增加重绘防重入保护。

## 明确不纳入本版

- 资源库独立搜索与三级快捷筛选 #73 尚未进入 `main`，v1.2.1 不宣称交付该能力。
- 不新增资源库正式素材和标签映射。
- 不修改 Electron 主进程、preload、本地 IPC、客户端依赖或打包配置。

## 验证记录

### 自动检查

| 检查 | 结果 |
| --- | --- |
| `pnpm lint` | 通过 |
| `pnpm test` | 通过，189 项全部通过 |
| `pnpm build` | 通过；保留既有大包体积与动态导入提示 |
| GitHub Actions `verify` | 待发布 PR 执行 |
| Windows 安装包工作流 | 待 `v1.2.1` Tag 和 Release 创建后执行 |

### 真实体验验收

- #92 / PR #93 已完成文本创建、编辑、排版、预览与导出验收。
- #94 / PR #96 已完成 Electron 作业预览的分数输入、焦点切换、嵌套禁用和整块删除验收。
- Windows NSIS 首次安装、覆盖安装和关键 IPC 验收待安装包生成后完成。

## 已知限制与部署

- 当前内部服务仍依赖 `pnpm dev`，没有 Windows 服务守护和自动重启。
- 公司台式机必须部署本版本对应的 `v1.2.1` Tag，并沿用 v1.2.0 使用的素材快照。
- 现有老师端无需重装，服务器升级后关闭并重新启动客户端即可加载本版能力。
- v1.2.1 Windows 安装包供新电脑首次安装和后续统一版本使用，不是现有老师端使用本版功能的前置条件。
- 部署前必须确认 `v1.2.0` 安装包、素材快照和运行数据备份仍可回退。
- 未完成 Windows 实际安装验收前，不向老师分发新安装包。

## 关联记录

- Version Issue：[#97](https://github.com/RexYoung000/editor/issues/97)
- 文本编辑：[#92](https://github.com/RexYoung000/editor/issues/92) / [PR #93](https://github.com/RexYoung000/editor/pull/93)
- 分数输入：[#94](https://github.com/RexYoung000/editor/issues/94) / [PR #96](https://github.com/RexYoung000/editor/pull/96)
- 发布 PR：待创建
- GitHub Release：待创建
